## Diagnosis\n\nThe `/alternatives-coforge/` base path is **not** in `vite.config.ts`. It is injected at build time by `.github/workflows/static.yml` (lines 38-43), which `sed`-patches both `vite.config.ts` and `src/App.tsx` before running `npm run build`. This was correct when the site lived at `https://<user>.github.io/alternatives-coforge/`, but for a root-domain custom domain like `nt-alternatives.com`, both the Vite `base` and the React Router `basename` must be `/`.\n\nEditing `vite.config.ts` directly would do nothing — the workflow would re-inject the bad value on the next push. The fix has to be in the workflow.\n\n## Changes\n\n### 1. `.github/workflows/static.yml` — remove the two injection steps\n\nDelete the steps that mutate `vite.config.ts` and `src/App.tsx`:\n- Remove **\